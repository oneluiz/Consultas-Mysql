-- ------------------------------------------------
-- Actualizar parte del string de un campo
-- -----------------------------------------------
UPDATE `tabla` SET `campo` = REPLACE ( `campo`, '/', '-' );
